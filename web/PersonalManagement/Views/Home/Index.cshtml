@model Portal_Posts_IndexViewModel
    @{
        ViewData["Title"] = "Home Page";

    }
    <div class="row">
        <div class="col-lg-8 col-md-10 mx-auto">
            @foreach (var post in Model.Posts.DataSource)
            {
                <div class="post-preview">
                    <a href="post.html">
                        <h2 class="post-title">
                            @post.Title
                        </h2>
                        <h3 class="post-subtitle">
                            @string.Join(", ", post.TagNames)
                        </h3>
                    </a>
                    <p class="post-meta">
                        Posted by
                        <a href="#">@post.Author</a>
                        on @post.CreatedOn
                    </p>
                </div>
                <hr>
            }
            <div class="clearfix">
                <a class="btn btn-primary float-right" href="#">Older Posts &rarr;</a>
            </div>
        </div>
    </div>
@*@model ChartViewModel

<style>
    .highcharts-figure,
    .highcharts-data-table table {
        min-width: 360px;
        max-width: 800px;
        margin: 1em auto;
    }

    .highcharts-data-table table {
        font-family: Verdana, sans-serif;
        border-collapse: collapse;
        border: 1px solid #ebebeb;
        margin: 10px auto;
        text-align: center;
        width: 100%;
        max-width: 500px;
    }

    .highcharts-data-table caption {
        padding: 1em 0;
        font-size: 1.2em;
        color: #555;
    }

    .highcharts-data-table th {
        font-weight: 600;
        padding: 0.5em;
    }

    .highcharts-data-table td,
    .highcharts-data-table th,
    .highcharts-data-table caption {
        padding: 0.5em;
    }

    .highcharts-data-table thead tr,
    .highcharts-data-table tr:nth-child(even) {
        background: #f8f8f8;
    }

    .highcharts-data-table tr:hover {
        background: #f1f7ff;
    }
</style>

@foreach (var rates in Model.Rates)
{
    @foreach (var rate in rates)
    {
        var id = rate.Match + "_" + rate.Site;
        var containerId = "container_" + id;
        var containerSpreadId = "container_spread_" + id;

<div class="row">
    <div class="col" id="@containerId"></div>
    <div class="col" id="@containerSpreadId"></div>
</div>


    }
}

@section Scripts {
    <script src="https://code.highcharts.com/highcharts.js"></script>
    <script src="https://code.highcharts.com/modules/series-label.js"></script>
    <script src="https://code.highcharts.com/modules/exporting.js"></script>
    <script src="https://code.highcharts.com/modules/export-data.js"></script>
    <script src="https://code.highcharts.com/modules/accessibility.js"></script>
    <script>
    $(document).ready(function () {
        @foreach (var rates in Model.Rates)
        {
            @foreach (var rate in rates)
            {
            <text>
        Highcharts.chart('container_' + '@rate.Match' + "_" + '@rate.Site', {
            title: {
                text: '@rate.Match'
            },

            yAxis: {
                title: {
                    text: 'Point'
                }
            },

            //xAxis: {
            //    accessibility: {
            //        rangeDescription: 'Range: 2010 to 2017'
            //    }
            //},

            legend: {
                layout: 'vertical',
                align: 'right',
                verticalAlign: 'middle'
            },

            plotOptions: {
                series: {
                    label: {
                        connectorAllowed: false
                    },
                    pointStart: 2010
                }
            },

            series: [{
                name: 'Total',
                data: [@String.Join(',', rate.Total)]
            }, {
                name: 'Over',
                data: [@String.Join(',', rate.OverReal)]
            }, {
                name: 'Under',
                data: [@String.Join(',', rate.UnderReal)]
            }],

            responsive: {
                rules: [{
                    condition: {
                        maxWidth: 500
                    },
                    chartOptions: {
                        legend: {
                            layout: 'horizontal',
                            align: 'center',
                            verticalAlign: 'bottom'
                        }
                    }
                }]
            }

        })

        Highcharts.chart('container_spread_' + '@rate.Match' + "_" + '@rate.Site', {
            title: {
                text: 'Spread'
            },

            yAxis: {
                title: {
                    text: 'Point'
                }
            },

            //xAxis: {
            //    accessibility: {
            //        rangeDescription: 'Range: 2010 to 2017'
            //    }
            //},

            legend: {
                layout: 'vertical',
                align: 'right',
                verticalAlign: 'middle'
            },

            plotOptions: {
                series: {
                    label: {
                        connectorAllowed: false
                    },
                    pointStart: 2010
                }
            },

            series: [{
                name: 'Spread',
                data: [@String.Join(',', rate.Spread)]
            }, {
                name: 'Team 1',
                data: [@String.Join(',', rate.Team1)]
            }, {
                name: 'Team 2',
                data: [@String.Join(',', rate.Team2)]
            }],

            responsive: {
                rules: [{
                    condition: {
                        maxWidth: 500
                    },
                    chartOptions: {
                        legend: {
                            layout: 'horizontal',
                            align: 'center',
                            verticalAlign: 'bottom'
                        }
                    }
                }]
            }

        })
            </text>
            }
        }
    })
    </script>
}*@
